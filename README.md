# Basta Desserts

This repository contains a small dessert website. The original static pages remain in the project root.
A React-based version lives in the `react-app` folder. Open `react-app/index.html` in a browser to explore it. The `react-app/styles` directory contains copies of the original CSS files so the React pages share the same styling.
